package lexer

import org.graphite.token.TokenType

class LexerTestHelper {
    fun testLexer(expectedTokenType: TokenType, expectedLiteral: String) {
//        assert(token.type == expectedTokenType) {
//            "Wrong token type! expected `$expectedTokenType`, got `${token.type}`"
//        }
//        assert(token.literal == expectedLiteral) {
//            "Wrong literal! expected `$expectedLiteral`, got `${token.literal}`"
//        }
//
//        println(token)
    }
}